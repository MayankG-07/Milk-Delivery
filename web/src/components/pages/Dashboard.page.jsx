import { Box } from "@mui/material";

export const Dashboard = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        mt: 10,
      }}
    >
      <h1>Dashboard</h1>
    </Box>
  );
};
