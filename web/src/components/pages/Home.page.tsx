import { Box } from "@mui/material";

export const Home = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        mt: 10,
      }}
    >
      <h1>Home</h1>
    </Box>
  );
};
