import { Box } from "@mui/material";

export const VerifyEmail = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        mt: 10,
      }}
    >
      <h1>Verify Email</h1>
    </Box>
  );
};
